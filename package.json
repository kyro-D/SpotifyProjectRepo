{
  "name": "spotify-project-repo",

  "scripts": {
    "start": "node backend/app.js",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run start"
  }
}
